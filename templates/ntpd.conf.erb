# $OpenBSD: ntpd.conf,v 1.11 2009/05/18 16:13:48 stevesk Exp $
# sample ntpd configuration file, see ntpd.conf(5)

# Addresses to listen on (ntpd does not listen by default)
<%= @listenon %>

# sync to a single server
#server ntp.example.org
<% if @server and @server.is_a? Array -%>
<% @server.each do |srv| -%><%= "server #{srv}\n" %><% end -%>
<% elsif @server -%><%=   "server #{@server}\n" %><% end -%>
>>>>>>> Add a params.pp, to define default values, and add servers entry
# use a random selection of NTP Pool Time Servers
# see http://support.ntp.org/bin/view/Servers/NTPPoolServers
<% if @servers and @servers.is_a? Array -%>
<% @servers.each do |srv| -%><%= "servers #{srv}\n" %><% end -%>
<% elsif @servers -%><%=   "servers #{@servers}\n" %><% end -%>

# use a specific local timedelta sensor (radio clock, etc)
#sensor nmea0

# use all detected timedelta sensors
#sensor *
